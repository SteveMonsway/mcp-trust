# The public benchmark output (452 generated HTML/Markdown reports + index) is
# machine-generated by `pnpm reports:public`. Mark it generated so GitHub Linguist
# excludes it from the repository language stats — otherwise the ~13 MB of report
# HTML makes a TypeScript project read as ~97% HTML — and collapses its diffs.
public-reports/** linguist-generated=true
